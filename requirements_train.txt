# requirements_train.txt — Colab 학습 전용
# 용도: train.py / predict.py 실행 (GPU 학습 포함)
# 설치: pip install -r requirements_train.txt -q
# ※ Streamlit Cloud에는 사용 불가 (tensorflow 포함)

yfinance>=0.2.40
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
lightgbm>=4.0.0
tensorflow>=2.13.0
plotly>=5.15.0
requests>=2.28.0
schedule>=1.2.0
